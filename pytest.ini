[pytest]
DJANGO_SETTINGS_MODULE = project.settings
django_find_project = false
pythonpath = FinalEclipse/project src/synapse
python_files = test_*.py
testpaths = tests
asyncio_mode = auto
markers =
    slow: downloads a model or otherwise takes a long time
filterwarnings =
    ignore::DeprecationWarning
